// .prettierrc.js
export default {
  // Use single quotes instead of double quotes for strings
  singleQuote: true,
  // Do not add semicolons at the end of statements
  semi: false,
  // Print width: wrap lines at 100 characters (common for readability)
  printWidth: 100,
  // Use 2 spaces for indentation
  tabWidth: 2,
  // Add trailing commas where valid in ES5 (objects, arrays, etc.)
  trailingComma: 'es5',
  // Use "always" to ensure parentheses around single arrow function parameters
  arrowParens: 'always',
  // Keep JSX closing bracket on the same line as the last attribute
  bracketSameLine: false,
  // Use single quotes for JSX attributes
  jsxSingleQuote: false,
  // Ensure consistent line endings across different operating systems
  endOfLine: 'lf',
}
